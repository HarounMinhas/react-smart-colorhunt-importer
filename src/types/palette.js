/**
 * @typedef {Object} Palette
 * @property {number} id - Unique identifier (timestamp)
 * @property {string} url - Original ColorHunt URL
 * @property {string[]} colors - Array of hex color codes (e.g., ["#1C352D"])
 * @property {string} colorKey - Unique key for duplicate detection
 * @property {string} timestamp - Human-readable timestamp
 */

/**
 * @typedef {'inline' | 'widget'} VariantType
 */

export {};
